<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>EJS</title>
  </head>
  <body>
    <% if (day == "Weekday") { %>
    <h2>Hey! It's a Weekday, it's time to work hard!</h2>
    <% } if (day == "Weekend") {%>
    <h2>Hey! It's the Weekend, it's time to have fun!</h2>
    <% } %>
  </body>
</html>
